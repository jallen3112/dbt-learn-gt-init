models:
  - name: stg_stripe_payments
    description: Staging model for stripe payments 
    columns:
          - name: id
            description: payment id primary map_keys
          - name: order_id
            description: order id foreign key
          - name: paymentmethod
            description: "{{doc('order_status')}}"